#include <stdio.h>

int main() {
    int x = 87;
    printf("character is %c",x);
    return 0;
}

/*
the ASCII character set uses a 7-bit binary code to represent a total of 128 characters, including letters, numbers, symbols, and control characters.

Here are some important ranges of ASCII character codes:

The lowercase letters a to z have ASCII codes ranging from 97 to 122.
The uppercase letters A to Z have ASCII codes ranging from 65 to 90.
The digits 0 to 9 have ASCII codes ranging from 48 to 57.
The whitespace characters space (' ') and tab ('\t') have ASCII codes 32 and 9, respectively.
*/