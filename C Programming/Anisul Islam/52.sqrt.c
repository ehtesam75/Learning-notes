#include <stdio.h>
#include <math.h>

int main() {
    //sqrt is in math.h
    printf("%lf", sqrt(49)); //need to use double type value for sqrt function, cuz sqrt returns a double type value 
    return 0;
}