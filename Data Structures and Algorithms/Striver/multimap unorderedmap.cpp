//multiset: can store duplicate keys but everything in sorted order 
//unordered map: will have uniqe keys and will not be sorted

//map works in log(n); but unorder map in most cases works in constant time o(1), but in wrost case (happen very hardly) in goes to o(n)